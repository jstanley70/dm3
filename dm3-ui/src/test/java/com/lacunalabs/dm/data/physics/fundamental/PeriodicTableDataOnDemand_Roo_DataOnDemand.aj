// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.PeriodicTable;
import com.lacunalabs.dm.data.physics.fundamental.PeriodicTableDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.fundamental.PeriodicTableService;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect PeriodicTableDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PeriodicTableDataOnDemand: @Component;
    
    private Random PeriodicTableDataOnDemand.rnd = new SecureRandom();
    
    private List<PeriodicTable> PeriodicTableDataOnDemand.data;
    
    @Autowired
    PeriodicTableService PeriodicTableDataOnDemand.periodicTableService;
    
    public PeriodicTable PeriodicTableDataOnDemand.getNewTransientPeriodicTable(int index) {
        PeriodicTable obj = new PeriodicTable();
        setName(obj, index);
        return obj;
    }
    
    public void PeriodicTableDataOnDemand.setName(PeriodicTable obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public PeriodicTable PeriodicTableDataOnDemand.getSpecificPeriodicTable(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        PeriodicTable obj = data.get(index);
        BigInteger id = obj.getId();
        return periodicTableService.findPeriodicTable(id);
    }
    
    public PeriodicTable PeriodicTableDataOnDemand.getRandomPeriodicTable() {
        init();
        PeriodicTable obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return periodicTableService.findPeriodicTable(id);
    }
    
    public boolean PeriodicTableDataOnDemand.modifyPeriodicTable(PeriodicTable obj) {
        return false;
    }
    
    public void PeriodicTableDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = periodicTableService.findPeriodicTableEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'PeriodicTable' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<PeriodicTable>();
        for (int i = 0; i < 10; i++) {
            PeriodicTable obj = getNewTransientPeriodicTable(i);
            try {
                periodicTableService.savePeriodicTable(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
