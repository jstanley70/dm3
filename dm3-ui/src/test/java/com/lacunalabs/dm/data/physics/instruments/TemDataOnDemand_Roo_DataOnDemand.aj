// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.instruments;

import com.lacunalabs.dm.data.physics.instruments.CcdCameraDataOnDemand;
import com.lacunalabs.dm.data.physics.instruments.Tem;
import com.lacunalabs.dm.data.physics.instruments.TemDataOnDemand;
import com.lacunalabs.dm.data.physics.instruments.TemDoubleTiltHolderDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.instruments.TemService;
import com.lacunalabs.dm.shared.physics.fundamental.MetaDataType;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect TemDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TemDataOnDemand: @Component;
    
    private Random TemDataOnDemand.rnd = new SecureRandom();
    
    private List<Tem> TemDataOnDemand.data;
    
    @Autowired
    private CcdCameraDataOnDemand TemDataOnDemand.ccdCameraDataOnDemand;
    
    @Autowired
    private TemDoubleTiltHolderDataOnDemand TemDataOnDemand.temDoubleTiltHolderDataOnDemand;
    
    @Autowired
    TemService TemDataOnDemand.temService;
    
    public Tem TemDataOnDemand.getNewTransientTem(int index) {
        Tem obj = new Tem();
        setManufacturer(obj, index);
        setMetadata(obj, index);
        setMetadataType(obj, index);
        setMicroscope(obj, index);
        return obj;
    }
    
    public void TemDataOnDemand.setManufacturer(Tem obj, int index) {
        String manufacturer = "manufacturer_" + index;
        obj.setManufacturer(manufacturer);
    }
    
    public void TemDataOnDemand.setMetadata(Tem obj, int index) {
        String metadata = "metadata_" + index;
        obj.setMetadata(metadata);
    }
    
    public void TemDataOnDemand.setMetadataType(Tem obj, int index) {
        MetaDataType metadataType = MetaDataType.class.getEnumConstants()[0];
        obj.setMetadataType(metadataType);
    }
    
    public void TemDataOnDemand.setMicroscope(Tem obj, int index) {
        String microscope = "microscope_" + index;
        obj.setMicroscope(microscope);
    }
    
    public Tem TemDataOnDemand.getSpecificTem(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Tem obj = data.get(index);
        BigInteger id = obj.getId();
        return temService.findTem(id);
    }
    
    public Tem TemDataOnDemand.getRandomTem() {
        init();
        Tem obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return temService.findTem(id);
    }
    
    public boolean TemDataOnDemand.modifyTem(Tem obj) {
        return false;
    }
    
    public void TemDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = temService.findTemEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Tem' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Tem>();
        for (int i = 0; i < 10; i++) {
            Tem obj = getNewTransientTem(i);
            try {
                temService.saveTem(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
