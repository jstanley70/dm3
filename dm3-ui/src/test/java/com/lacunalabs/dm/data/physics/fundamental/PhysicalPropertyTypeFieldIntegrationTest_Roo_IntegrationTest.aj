// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.PhysicalPropertyTypeFieldDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.PhysicalPropertyTypeFieldIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.PhysicalPropertyTypeFieldService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect PhysicalPropertyTypeFieldIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PhysicalPropertyTypeFieldIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PhysicalPropertyTypeFieldIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private PhysicalPropertyTypeFieldDataOnDemand PhysicalPropertyTypeFieldIntegrationTest.dod;
    
    @Autowired
    PhysicalPropertyTypeFieldService PhysicalPropertyTypeFieldIntegrationTest.physicalPropertyTypeFieldService;
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testCountAllPhysicalPropertyTypeFields() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", dod.getRandomPhysicalPropertyTypeField());
        long count = physicalPropertyTypeFieldService.countAllPhysicalPropertyTypeFields();
        Assert.assertTrue("Counter for 'PhysicalPropertyTypeField' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testFindPhysicalPropertyTypeField() {
        PhysicalPropertyTypeField obj = dod.getRandomPhysicalPropertyTypeField();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to provide an identifier", id);
        obj = physicalPropertyTypeFieldService.findPhysicalPropertyTypeField(id);
        Assert.assertNotNull("Find method for 'PhysicalPropertyTypeField' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PhysicalPropertyTypeField' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testFindAllPhysicalPropertyTypeFields() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", dod.getRandomPhysicalPropertyTypeField());
        long count = physicalPropertyTypeFieldService.countAllPhysicalPropertyTypeFields();
        Assert.assertTrue("Too expensive to perform a find all test for 'PhysicalPropertyTypeField', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PhysicalPropertyTypeField> result = physicalPropertyTypeFieldService.findAllPhysicalPropertyTypeFields();
        Assert.assertNotNull("Find all method for 'PhysicalPropertyTypeField' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PhysicalPropertyTypeField' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testFindPhysicalPropertyTypeFieldEntries() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", dod.getRandomPhysicalPropertyTypeField());
        long count = physicalPropertyTypeFieldService.countAllPhysicalPropertyTypeFields();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PhysicalPropertyTypeField> result = physicalPropertyTypeFieldService.findPhysicalPropertyTypeFieldEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PhysicalPropertyTypeField' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PhysicalPropertyTypeField' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testSavePhysicalPropertyTypeField() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", dod.getRandomPhysicalPropertyTypeField());
        PhysicalPropertyTypeField obj = dod.getNewTransientPhysicalPropertyTypeField(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PhysicalPropertyTypeField' identifier to be null", obj.getId());
        physicalPropertyTypeFieldService.savePhysicalPropertyTypeField(obj);
        Assert.assertNotNull("Expected 'PhysicalPropertyTypeField' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PhysicalPropertyTypeFieldIntegrationTest.testDeletePhysicalPropertyTypeField() {
        PhysicalPropertyTypeField obj = dod.getRandomPhysicalPropertyTypeField();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyTypeField' failed to provide an identifier", id);
        obj = physicalPropertyTypeFieldService.findPhysicalPropertyTypeField(id);
        physicalPropertyTypeFieldService.deletePhysicalPropertyTypeField(obj);
        Assert.assertNull("Failed to remove 'PhysicalPropertyTypeField' with identifier '" + id + "'", physicalPropertyTypeFieldService.findPhysicalPropertyTypeField(id));
    }
    
}
