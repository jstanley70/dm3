// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.experiment;

import com.lacunalabs.dm.data.physics.experiment.ProcessorDataOnDemand;
import com.lacunalabs.dm.data.physics.experiment.ProcessorIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.experiment.ProcessorService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect ProcessorIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProcessorIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProcessorIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private ProcessorDataOnDemand ProcessorIntegrationTest.dod;
    
    @Autowired
    ProcessorService ProcessorIntegrationTest.processorService;
    
    @Test
    public void ProcessorIntegrationTest.testCountAllProcessors() {
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", dod.getRandomProcessor());
        long count = processorService.countAllProcessors();
        Assert.assertTrue("Counter for 'Processor' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProcessorIntegrationTest.testFindProcessor() {
        Processor obj = dod.getRandomProcessor();
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Processor' failed to provide an identifier", id);
        obj = processorService.findProcessor(id);
        Assert.assertNotNull("Find method for 'Processor' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Processor' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProcessorIntegrationTest.testFindAllProcessors() {
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", dod.getRandomProcessor());
        long count = processorService.countAllProcessors();
        Assert.assertTrue("Too expensive to perform a find all test for 'Processor', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Processor> result = processorService.findAllProcessors();
        Assert.assertNotNull("Find all method for 'Processor' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Processor' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProcessorIntegrationTest.testFindProcessorEntries() {
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", dod.getRandomProcessor());
        long count = processorService.countAllProcessors();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Processor> result = processorService.findProcessorEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Processor' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Processor' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProcessorIntegrationTest.testSaveProcessor() {
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", dod.getRandomProcessor());
        Processor obj = dod.getNewTransientProcessor(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Processor' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Processor' identifier to be null", obj.getId());
        processorService.saveProcessor(obj);
        Assert.assertNotNull("Expected 'Processor' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProcessorIntegrationTest.testDeleteProcessor() {
        Processor obj = dod.getRandomProcessor();
        Assert.assertNotNull("Data on demand for 'Processor' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Processor' failed to provide an identifier", id);
        obj = processorService.findProcessor(id);
        processorService.deleteProcessor(obj);
        Assert.assertNull("Failed to remove 'Processor' with identifier '" + id + "'", processorService.findProcessor(id));
    }
    
}
