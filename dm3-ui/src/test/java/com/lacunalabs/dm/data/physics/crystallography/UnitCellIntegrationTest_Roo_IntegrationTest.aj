// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.crystallography;

import com.lacunalabs.dm.data.physics.crystallography.UnitCellDataOnDemand;
import com.lacunalabs.dm.data.physics.crystallography.UnitCellIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.crystallography.UnitCellService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect UnitCellIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UnitCellIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UnitCellIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private UnitCellDataOnDemand UnitCellIntegrationTest.dod;
    
    @Autowired
    UnitCellService UnitCellIntegrationTest.unitCellService;
    
    @Test
    public void UnitCellIntegrationTest.testCountAllUnitCells() {
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", dod.getRandomUnitCell());
        long count = unitCellService.countAllUnitCells();
        Assert.assertTrue("Counter for 'UnitCell' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UnitCellIntegrationTest.testFindUnitCell() {
        UnitCell obj = dod.getRandomUnitCell();
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to provide an identifier", id);
        obj = unitCellService.findUnitCell(id);
        Assert.assertNotNull("Find method for 'UnitCell' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UnitCell' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UnitCellIntegrationTest.testFindAllUnitCells() {
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", dod.getRandomUnitCell());
        long count = unitCellService.countAllUnitCells();
        Assert.assertTrue("Too expensive to perform a find all test for 'UnitCell', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UnitCell> result = unitCellService.findAllUnitCells();
        Assert.assertNotNull("Find all method for 'UnitCell' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UnitCell' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UnitCellIntegrationTest.testFindUnitCellEntries() {
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", dod.getRandomUnitCell());
        long count = unitCellService.countAllUnitCells();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UnitCell> result = unitCellService.findUnitCellEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UnitCell' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UnitCell' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UnitCellIntegrationTest.testSaveUnitCell() {
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", dod.getRandomUnitCell());
        UnitCell obj = dod.getNewTransientUnitCell(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UnitCell' identifier to be null", obj.getId());
        unitCellService.saveUnitCell(obj);
        Assert.assertNotNull("Expected 'UnitCell' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UnitCellIntegrationTest.testDeleteUnitCell() {
        UnitCell obj = dod.getRandomUnitCell();
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UnitCell' failed to provide an identifier", id);
        obj = unitCellService.findUnitCell(id);
        unitCellService.deleteUnitCell(obj);
        Assert.assertNull("Failed to remove 'UnitCell' with identifier '" + id + "'", unitCellService.findUnitCell(id));
    }
    
}
