// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.ParticlePhysicalPropertyDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.ParticlePhysicalPropertyIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.ParticlePhysicalPropertyService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect ParticlePhysicalPropertyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ParticlePhysicalPropertyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ParticlePhysicalPropertyIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private ParticlePhysicalPropertyDataOnDemand ParticlePhysicalPropertyIntegrationTest.dod;
    
    @Autowired
    ParticlePhysicalPropertyService ParticlePhysicalPropertyIntegrationTest.particlePhysicalPropertyService;
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testCountAllParticlePhysicalPropertys() {
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", dod.getRandomParticlePhysicalProperty());
        long count = particlePhysicalPropertyService.countAllParticlePhysicalPropertys();
        Assert.assertTrue("Counter for 'ParticlePhysicalProperty' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testFindParticlePhysicalProperty() {
        ParticlePhysicalProperty obj = dod.getRandomParticlePhysicalProperty();
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to provide an identifier", id);
        obj = particlePhysicalPropertyService.findParticlePhysicalProperty(id);
        Assert.assertNotNull("Find method for 'ParticlePhysicalProperty' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ParticlePhysicalProperty' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testFindAllParticlePhysicalPropertys() {
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", dod.getRandomParticlePhysicalProperty());
        long count = particlePhysicalPropertyService.countAllParticlePhysicalPropertys();
        Assert.assertTrue("Too expensive to perform a find all test for 'ParticlePhysicalProperty', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ParticlePhysicalProperty> result = particlePhysicalPropertyService.findAllParticlePhysicalPropertys();
        Assert.assertNotNull("Find all method for 'ParticlePhysicalProperty' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ParticlePhysicalProperty' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testFindParticlePhysicalPropertyEntries() {
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", dod.getRandomParticlePhysicalProperty());
        long count = particlePhysicalPropertyService.countAllParticlePhysicalPropertys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ParticlePhysicalProperty> result = particlePhysicalPropertyService.findParticlePhysicalPropertyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ParticlePhysicalProperty' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ParticlePhysicalProperty' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testSaveParticlePhysicalProperty() {
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", dod.getRandomParticlePhysicalProperty());
        ParticlePhysicalProperty obj = dod.getNewTransientParticlePhysicalProperty(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ParticlePhysicalProperty' identifier to be null", obj.getId());
        particlePhysicalPropertyService.saveParticlePhysicalProperty(obj);
        Assert.assertNotNull("Expected 'ParticlePhysicalProperty' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ParticlePhysicalPropertyIntegrationTest.testDeleteParticlePhysicalProperty() {
        ParticlePhysicalProperty obj = dod.getRandomParticlePhysicalProperty();
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParticlePhysicalProperty' failed to provide an identifier", id);
        obj = particlePhysicalPropertyService.findParticlePhysicalProperty(id);
        particlePhysicalPropertyService.deleteParticlePhysicalProperty(obj);
        Assert.assertNull("Failed to remove 'ParticlePhysicalProperty' with identifier '" + id + "'", particlePhysicalPropertyService.findParticlePhysicalProperty(id));
    }
    
}
