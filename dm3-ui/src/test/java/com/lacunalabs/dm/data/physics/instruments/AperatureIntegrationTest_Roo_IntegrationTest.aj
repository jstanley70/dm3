// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.instruments;

import com.lacunalabs.dm.data.physics.instruments.AperatureDataOnDemand;
import com.lacunalabs.dm.data.physics.instruments.AperatureIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.instruments.AperatureService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect AperatureIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AperatureIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AperatureIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private AperatureDataOnDemand AperatureIntegrationTest.dod;
    
    @Autowired
    AperatureService AperatureIntegrationTest.aperatureService;
    
    @Test
    public void AperatureIntegrationTest.testCountAllAperatures() {
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", dod.getRandomAperature());
        long count = aperatureService.countAllAperatures();
        Assert.assertTrue("Counter for 'Aperature' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AperatureIntegrationTest.testFindAperature() {
        Aperature obj = dod.getRandomAperature();
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Aperature' failed to provide an identifier", id);
        obj = aperatureService.findAperature(id);
        Assert.assertNotNull("Find method for 'Aperature' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Aperature' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AperatureIntegrationTest.testFindAllAperatures() {
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", dod.getRandomAperature());
        long count = aperatureService.countAllAperatures();
        Assert.assertTrue("Too expensive to perform a find all test for 'Aperature', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Aperature> result = aperatureService.findAllAperatures();
        Assert.assertNotNull("Find all method for 'Aperature' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Aperature' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AperatureIntegrationTest.testFindAperatureEntries() {
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", dod.getRandomAperature());
        long count = aperatureService.countAllAperatures();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Aperature> result = aperatureService.findAperatureEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Aperature' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Aperature' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AperatureIntegrationTest.testSaveAperature() {
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", dod.getRandomAperature());
        Aperature obj = dod.getNewTransientAperature(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Aperature' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Aperature' identifier to be null", obj.getId());
        aperatureService.saveAperature(obj);
        Assert.assertNotNull("Expected 'Aperature' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AperatureIntegrationTest.testDeleteAperature() {
        Aperature obj = dod.getRandomAperature();
        Assert.assertNotNull("Data on demand for 'Aperature' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Aperature' failed to provide an identifier", id);
        obj = aperatureService.findAperature(id);
        aperatureService.deleteAperature(obj);
        Assert.assertNull("Failed to remove 'Aperature' with identifier '" + id + "'", aperatureService.findAperature(id));
    }
    
}
