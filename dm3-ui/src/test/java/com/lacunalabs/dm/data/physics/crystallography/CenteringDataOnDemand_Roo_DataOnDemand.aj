// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.crystallography;

import com.lacunalabs.dm.data.physics.crystallography.Centering;
import com.lacunalabs.dm.data.physics.crystallography.CenteringDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.crystallography.CenteringService;
import com.lacunalabs.dm.shared.physics.crystallography.CenteringType;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect CenteringDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CenteringDataOnDemand: @Component;
    
    private Random CenteringDataOnDemand.rnd = new SecureRandom();
    
    private List<Centering> CenteringDataOnDemand.data;
    
    @Autowired
    CenteringService CenteringDataOnDemand.centeringService;
    
    public Centering CenteringDataOnDemand.getNewTransientCentering(int index) {
        Centering obj = new Centering();
        setCenteringType(obj, index);
        setCenterings(obj, index);
        setName(obj, index);
        setReflectionConditions(obj, index);
        return obj;
    }
    
    public void CenteringDataOnDemand.setCenteringType(Centering obj, int index) {
        CenteringType centeringType = CenteringType.class.getEnumConstants()[0];
        obj.setCenteringType(centeringType);
    }
    
    public void CenteringDataOnDemand.setCenterings(Centering obj, int index) {
        String centerings = "centerings_" + index;
        obj.setCenterings(centerings);
    }
    
    public void CenteringDataOnDemand.setName(Centering obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void CenteringDataOnDemand.setReflectionConditions(Centering obj, int index) {
        String reflectionConditions = "reflectionConditions_" + index;
        obj.setReflectionConditions(reflectionConditions);
    }
    
    public Centering CenteringDataOnDemand.getSpecificCentering(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Centering obj = data.get(index);
        BigInteger id = obj.getId();
        return centeringService.findCentering(id);
    }
    
    public Centering CenteringDataOnDemand.getRandomCentering() {
        init();
        Centering obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return centeringService.findCentering(id);
    }
    
    public boolean CenteringDataOnDemand.modifyCentering(Centering obj) {
        return false;
    }
    
    public void CenteringDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = centeringService.findCenteringEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Centering' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Centering>();
        for (int i = 0; i < 10; i++) {
            Centering obj = getNewTransientCentering(i);
            try {
                centeringService.saveCentering(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
