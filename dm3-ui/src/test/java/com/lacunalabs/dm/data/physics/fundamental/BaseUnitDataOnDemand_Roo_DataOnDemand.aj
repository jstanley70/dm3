// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.BaseUnit;
import com.lacunalabs.dm.data.physics.fundamental.BaseUnitDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.DecimalMultiplierDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.fundamental.BaseUnitService;
import com.lacunalabs.dm.shared.physics.fundamental.UnitType;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect BaseUnitDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BaseUnitDataOnDemand: @Component;
    
    private Random BaseUnitDataOnDemand.rnd = new SecureRandom();
    
    private List<BaseUnit> BaseUnitDataOnDemand.data;
    
    @Autowired
    private DecimalMultiplierDataOnDemand BaseUnitDataOnDemand.decimalMultiplierDataOnDemand;
    
    @Autowired
    BaseUnitService BaseUnitDataOnDemand.baseUnitService;
    
    public BaseUnit BaseUnitDataOnDemand.getNewTransientBaseUnit(int index) {
        BaseUnit obj = new BaseUnit();
        setBase_quantity(obj, index);
        setName(obj, index);
        setSymbol(obj, index);
        return obj;
    }
    
    public void BaseUnitDataOnDemand.setBase_quantity(BaseUnit obj, int index) {
        UnitType base_quantity = UnitType.class.getEnumConstants()[0];
        obj.setBase_quantity(base_quantity);
    }
    
    public void BaseUnitDataOnDemand.setName(BaseUnit obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public void BaseUnitDataOnDemand.setSymbol(BaseUnit obj, int index) {
        String symbol = "symbol_" + index;
        if (symbol.length() > 10) {
            symbol = symbol.substring(0, 10);
        }
        obj.setSymbol(symbol);
    }
    
    public BaseUnit BaseUnitDataOnDemand.getSpecificBaseUnit(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        BaseUnit obj = data.get(index);
        BigInteger id = obj.getId();
        return baseUnitService.findBaseUnit(id);
    }
    
    public BaseUnit BaseUnitDataOnDemand.getRandomBaseUnit() {
        init();
        BaseUnit obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return baseUnitService.findBaseUnit(id);
    }
    
    public boolean BaseUnitDataOnDemand.modifyBaseUnit(BaseUnit obj) {
        return false;
    }
    
    public void BaseUnitDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = baseUnitService.findBaseUnitEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'BaseUnit' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<BaseUnit>();
        for (int i = 0; i < 10; i++) {
            BaseUnit obj = getNewTransientBaseUnit(i);
            try {
                baseUnitService.saveBaseUnit(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
