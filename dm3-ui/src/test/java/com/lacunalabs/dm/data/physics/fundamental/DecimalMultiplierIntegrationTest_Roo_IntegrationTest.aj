// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.DecimalMultiplierDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.DecimalMultiplierIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.DecimalMultiplierService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect DecimalMultiplierIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DecimalMultiplierIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DecimalMultiplierIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private DecimalMultiplierDataOnDemand DecimalMultiplierIntegrationTest.dod;
    
    @Autowired
    DecimalMultiplierService DecimalMultiplierIntegrationTest.decimalMultiplierService;
    
    @Test
    public void DecimalMultiplierIntegrationTest.testCountAllDecimalMultipliers() {
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", dod.getRandomDecimalMultiplier());
        long count = decimalMultiplierService.countAllDecimalMultipliers();
        Assert.assertTrue("Counter for 'DecimalMultiplier' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DecimalMultiplierIntegrationTest.testFindDecimalMultiplier() {
        DecimalMultiplier obj = dod.getRandomDecimalMultiplier();
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to provide an identifier", id);
        obj = decimalMultiplierService.findDecimalMultiplier(id);
        Assert.assertNotNull("Find method for 'DecimalMultiplier' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DecimalMultiplier' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DecimalMultiplierIntegrationTest.testFindAllDecimalMultipliers() {
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", dod.getRandomDecimalMultiplier());
        long count = decimalMultiplierService.countAllDecimalMultipliers();
        Assert.assertTrue("Too expensive to perform a find all test for 'DecimalMultiplier', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DecimalMultiplier> result = decimalMultiplierService.findAllDecimalMultipliers();
        Assert.assertNotNull("Find all method for 'DecimalMultiplier' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DecimalMultiplier' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DecimalMultiplierIntegrationTest.testFindDecimalMultiplierEntries() {
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", dod.getRandomDecimalMultiplier());
        long count = decimalMultiplierService.countAllDecimalMultipliers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DecimalMultiplier> result = decimalMultiplierService.findDecimalMultiplierEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DecimalMultiplier' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DecimalMultiplier' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DecimalMultiplierIntegrationTest.testSaveDecimalMultiplier() {
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", dod.getRandomDecimalMultiplier());
        DecimalMultiplier obj = dod.getNewTransientDecimalMultiplier(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DecimalMultiplier' identifier to be null", obj.getId());
        decimalMultiplierService.saveDecimalMultiplier(obj);
        Assert.assertNotNull("Expected 'DecimalMultiplier' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DecimalMultiplierIntegrationTest.testDeleteDecimalMultiplier() {
        DecimalMultiplier obj = dod.getRandomDecimalMultiplier();
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DecimalMultiplier' failed to provide an identifier", id);
        obj = decimalMultiplierService.findDecimalMultiplier(id);
        decimalMultiplierService.deleteDecimalMultiplier(obj);
        Assert.assertNull("Failed to remove 'DecimalMultiplier' with identifier '" + id + "'", decimalMultiplierService.findDecimalMultiplier(id));
    }
    
}
