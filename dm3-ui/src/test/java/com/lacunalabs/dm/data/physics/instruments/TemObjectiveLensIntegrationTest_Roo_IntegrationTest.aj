// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.instruments;

import com.lacunalabs.dm.data.physics.instruments.TemObjectiveLensDataOnDemand;
import com.lacunalabs.dm.data.physics.instruments.TemObjectiveLensIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.instruments.TemObjectiveLensService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect TemObjectiveLensIntegrationTest_Roo_IntegrationTest {
    
    declare @type: TemObjectiveLensIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: TemObjectiveLensIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private TemObjectiveLensDataOnDemand TemObjectiveLensIntegrationTest.dod;
    
    @Autowired
    TemObjectiveLensService TemObjectiveLensIntegrationTest.temObjectiveLensService;
    
    @Test
    public void TemObjectiveLensIntegrationTest.testCountAllTemObjectiveLenses() {
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", dod.getRandomTemObjectiveLens());
        long count = temObjectiveLensService.countAllTemObjectiveLenses();
        Assert.assertTrue("Counter for 'TemObjectiveLens' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void TemObjectiveLensIntegrationTest.testFindTemObjectiveLens() {
        TemObjectiveLens obj = dod.getRandomTemObjectiveLens();
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to provide an identifier", id);
        obj = temObjectiveLensService.findTemObjectiveLens(id);
        Assert.assertNotNull("Find method for 'TemObjectiveLens' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'TemObjectiveLens' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void TemObjectiveLensIntegrationTest.testFindAllTemObjectiveLenses() {
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", dod.getRandomTemObjectiveLens());
        long count = temObjectiveLensService.countAllTemObjectiveLenses();
        Assert.assertTrue("Too expensive to perform a find all test for 'TemObjectiveLens', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<TemObjectiveLens> result = temObjectiveLensService.findAllTemObjectiveLenses();
        Assert.assertNotNull("Find all method for 'TemObjectiveLens' illegally returned null", result);
        Assert.assertTrue("Find all method for 'TemObjectiveLens' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void TemObjectiveLensIntegrationTest.testFindTemObjectiveLensEntries() {
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", dod.getRandomTemObjectiveLens());
        long count = temObjectiveLensService.countAllTemObjectiveLenses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<TemObjectiveLens> result = temObjectiveLensService.findTemObjectiveLensEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'TemObjectiveLens' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'TemObjectiveLens' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void TemObjectiveLensIntegrationTest.testSaveTemObjectiveLens() {
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", dod.getRandomTemObjectiveLens());
        TemObjectiveLens obj = dod.getNewTransientTemObjectiveLens(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'TemObjectiveLens' identifier to be null", obj.getId());
        temObjectiveLensService.saveTemObjectiveLens(obj);
        Assert.assertNotNull("Expected 'TemObjectiveLens' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void TemObjectiveLensIntegrationTest.testDeleteTemObjectiveLens() {
        TemObjectiveLens obj = dod.getRandomTemObjectiveLens();
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'TemObjectiveLens' failed to provide an identifier", id);
        obj = temObjectiveLensService.findTemObjectiveLens(id);
        temObjectiveLensService.deleteTemObjectiveLens(obj);
        Assert.assertNull("Failed to remove 'TemObjectiveLens' with identifier '" + id + "'", temObjectiveLensService.findTemObjectiveLens(id));
    }
    
}
