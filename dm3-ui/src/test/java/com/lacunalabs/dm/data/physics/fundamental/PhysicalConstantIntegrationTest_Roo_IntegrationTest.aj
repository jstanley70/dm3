// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.PhysicalConstantDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.PhysicalConstantIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.PhysicalConstantService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect PhysicalConstantIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PhysicalConstantIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PhysicalConstantIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private PhysicalConstantDataOnDemand PhysicalConstantIntegrationTest.dod;
    
    @Autowired
    PhysicalConstantService PhysicalConstantIntegrationTest.physicalConstantService;
    
    @Test
    public void PhysicalConstantIntegrationTest.testCountAllPhysicalConstants() {
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", dod.getRandomPhysicalConstant());
        long count = physicalConstantService.countAllPhysicalConstants();
        Assert.assertTrue("Counter for 'PhysicalConstant' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PhysicalConstantIntegrationTest.testFindPhysicalConstant() {
        PhysicalConstant obj = dod.getRandomPhysicalConstant();
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to provide an identifier", id);
        obj = physicalConstantService.findPhysicalConstant(id);
        Assert.assertNotNull("Find method for 'PhysicalConstant' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PhysicalConstant' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PhysicalConstantIntegrationTest.testFindAllPhysicalConstants() {
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", dod.getRandomPhysicalConstant());
        long count = physicalConstantService.countAllPhysicalConstants();
        Assert.assertTrue("Too expensive to perform a find all test for 'PhysicalConstant', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PhysicalConstant> result = physicalConstantService.findAllPhysicalConstants();
        Assert.assertNotNull("Find all method for 'PhysicalConstant' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PhysicalConstant' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PhysicalConstantIntegrationTest.testFindPhysicalConstantEntries() {
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", dod.getRandomPhysicalConstant());
        long count = physicalConstantService.countAllPhysicalConstants();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PhysicalConstant> result = physicalConstantService.findPhysicalConstantEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PhysicalConstant' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PhysicalConstant' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PhysicalConstantIntegrationTest.testSavePhysicalConstant() {
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", dod.getRandomPhysicalConstant());
        PhysicalConstant obj = dod.getNewTransientPhysicalConstant(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PhysicalConstant' identifier to be null", obj.getId());
        physicalConstantService.savePhysicalConstant(obj);
        Assert.assertNotNull("Expected 'PhysicalConstant' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PhysicalConstantIntegrationTest.testDeletePhysicalConstant() {
        PhysicalConstant obj = dod.getRandomPhysicalConstant();
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalConstant' failed to provide an identifier", id);
        obj = physicalConstantService.findPhysicalConstant(id);
        physicalConstantService.deletePhysicalConstant(obj);
        Assert.assertNull("Failed to remove 'PhysicalConstant' with identifier '" + id + "'", physicalConstantService.findPhysicalConstant(id));
    }
    
}
