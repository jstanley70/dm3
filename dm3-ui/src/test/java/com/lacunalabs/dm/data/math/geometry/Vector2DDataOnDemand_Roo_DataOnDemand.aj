// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.math.geometry;

import com.lacunalabs.dm.data.math.geometry.Vector2D;
import com.lacunalabs.dm.data.math.geometry.Vector2DDataOnDemand;
import com.lacunalabs.dm.data.repository.math.geometry.Vector2DService;
import java.math.BigDecimal;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect Vector2DDataOnDemand_Roo_DataOnDemand {
    
    declare @type: Vector2DDataOnDemand: @Component;
    
    private Random Vector2DDataOnDemand.rnd = new SecureRandom();
    
    private List<Vector2D> Vector2DDataOnDemand.data;
    
    @Autowired
    Vector2DService Vector2DDataOnDemand.vector2DService;
    
    public Vector2D Vector2DDataOnDemand.getNewTransientVector2D(int index) {
        Vector2D obj = new Vector2D();
        setX(obj, index);
        setY(obj, index);
        return obj;
    }
    
    public void Vector2DDataOnDemand.setX(Vector2D obj, int index) {
        BigDecimal x = BigDecimal.valueOf(index);
        obj.setX(x);
    }
    
    public void Vector2DDataOnDemand.setY(Vector2D obj, int index) {
        BigDecimal y = BigDecimal.valueOf(index);
        obj.setY(y);
    }
    
    public Vector2D Vector2DDataOnDemand.getSpecificVector2D(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Vector2D obj = data.get(index);
        BigInteger id = obj.getId();
        return vector2DService.findVector2D(id);
    }
    
    public Vector2D Vector2DDataOnDemand.getRandomVector2D() {
        init();
        Vector2D obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return vector2DService.findVector2D(id);
    }
    
    public boolean Vector2DDataOnDemand.modifyVector2D(Vector2D obj) {
        return false;
    }
    
    public void Vector2DDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = vector2DService.findVector2DEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Vector2D' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Vector2D>();
        for (int i = 0; i < 10; i++) {
            Vector2D obj = getNewTransientVector2D(i);
            try {
                vector2DService.saveVector2D(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
