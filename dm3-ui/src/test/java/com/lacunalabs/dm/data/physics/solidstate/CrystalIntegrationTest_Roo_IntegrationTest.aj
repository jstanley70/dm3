// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.solidstate;

import com.lacunalabs.dm.data.physics.solidstate.CrystalDataOnDemand;
import com.lacunalabs.dm.data.physics.solidstate.CrystalIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.solidstate.CrystalService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect CrystalIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CrystalIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CrystalIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private CrystalDataOnDemand CrystalIntegrationTest.dod;
    
    @Autowired
    CrystalService CrystalIntegrationTest.crystalService;
    
    @Test
    public void CrystalIntegrationTest.testCountAllCrystals() {
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", dod.getRandomCrystal());
        long count = crystalService.countAllCrystals();
        Assert.assertTrue("Counter for 'Crystal' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CrystalIntegrationTest.testFindCrystal() {
        Crystal obj = dod.getRandomCrystal();
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Crystal' failed to provide an identifier", id);
        obj = crystalService.findCrystal(id);
        Assert.assertNotNull("Find method for 'Crystal' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Crystal' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CrystalIntegrationTest.testFindAllCrystals() {
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", dod.getRandomCrystal());
        long count = crystalService.countAllCrystals();
        Assert.assertTrue("Too expensive to perform a find all test for 'Crystal', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Crystal> result = crystalService.findAllCrystals();
        Assert.assertNotNull("Find all method for 'Crystal' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Crystal' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CrystalIntegrationTest.testFindCrystalEntries() {
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", dod.getRandomCrystal());
        long count = crystalService.countAllCrystals();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Crystal> result = crystalService.findCrystalEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Crystal' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Crystal' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CrystalIntegrationTest.testSaveCrystal() {
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", dod.getRandomCrystal());
        Crystal obj = dod.getNewTransientCrystal(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Crystal' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Crystal' identifier to be null", obj.getId());
        crystalService.saveCrystal(obj);
        Assert.assertNotNull("Expected 'Crystal' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CrystalIntegrationTest.testDeleteCrystal() {
        Crystal obj = dod.getRandomCrystal();
        Assert.assertNotNull("Data on demand for 'Crystal' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Crystal' failed to provide an identifier", id);
        obj = crystalService.findCrystal(id);
        crystalService.deleteCrystal(obj);
        Assert.assertNull("Failed to remove 'Crystal' with identifier '" + id + "'", crystalService.findCrystal(id));
    }
    
}
