// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.PhysicalPropertyTypeDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.PhysicalPropertyTypeIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.PhysicalPropertyTypeService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect PhysicalPropertyTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PhysicalPropertyTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PhysicalPropertyTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private PhysicalPropertyTypeDataOnDemand PhysicalPropertyTypeIntegrationTest.dod;
    
    @Autowired
    PhysicalPropertyTypeService PhysicalPropertyTypeIntegrationTest.physicalPropertyTypeService;
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testCountAllPhysicalPropertyTypes() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", dod.getRandomPhysicalPropertyType());
        long count = physicalPropertyTypeService.countAllPhysicalPropertyTypes();
        Assert.assertTrue("Counter for 'PhysicalPropertyType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testFindPhysicalPropertyType() {
        PhysicalPropertyType obj = dod.getRandomPhysicalPropertyType();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to provide an identifier", id);
        obj = physicalPropertyTypeService.findPhysicalPropertyType(id);
        Assert.assertNotNull("Find method for 'PhysicalPropertyType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PhysicalPropertyType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testFindAllPhysicalPropertyTypes() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", dod.getRandomPhysicalPropertyType());
        long count = physicalPropertyTypeService.countAllPhysicalPropertyTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'PhysicalPropertyType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PhysicalPropertyType> result = physicalPropertyTypeService.findAllPhysicalPropertyTypes();
        Assert.assertNotNull("Find all method for 'PhysicalPropertyType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PhysicalPropertyType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testFindPhysicalPropertyTypeEntries() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", dod.getRandomPhysicalPropertyType());
        long count = physicalPropertyTypeService.countAllPhysicalPropertyTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PhysicalPropertyType> result = physicalPropertyTypeService.findPhysicalPropertyTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PhysicalPropertyType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PhysicalPropertyType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testSavePhysicalPropertyType() {
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", dod.getRandomPhysicalPropertyType());
        PhysicalPropertyType obj = dod.getNewTransientPhysicalPropertyType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PhysicalPropertyType' identifier to be null", obj.getId());
        physicalPropertyTypeService.savePhysicalPropertyType(obj);
        Assert.assertNotNull("Expected 'PhysicalPropertyType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PhysicalPropertyTypeIntegrationTest.testDeletePhysicalPropertyType() {
        PhysicalPropertyType obj = dod.getRandomPhysicalPropertyType();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhysicalPropertyType' failed to provide an identifier", id);
        obj = physicalPropertyTypeService.findPhysicalPropertyType(id);
        physicalPropertyTypeService.deletePhysicalPropertyType(obj);
        Assert.assertNull("Failed to remove 'PhysicalPropertyType' with identifier '" + id + "'", physicalPropertyTypeService.findPhysicalPropertyType(id));
    }
    
}
