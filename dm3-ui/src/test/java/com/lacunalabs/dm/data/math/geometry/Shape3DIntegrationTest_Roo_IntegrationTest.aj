// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.math.geometry;

import com.lacunalabs.dm.data.math.geometry.Shape3DDataOnDemand;
import com.lacunalabs.dm.data.math.geometry.Shape3DIntegrationTest;
import com.lacunalabs.dm.data.repository.math.geometry.Shape3DService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect Shape3DIntegrationTest_Roo_IntegrationTest {
    
    declare @type: Shape3DIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: Shape3DIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private Shape3DDataOnDemand Shape3DIntegrationTest.dod;
    
    @Autowired
    Shape3DService Shape3DIntegrationTest.shape3DService;
    
    @Test
    public void Shape3DIntegrationTest.testCountAllShape3Ds() {
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", dod.getRandomShape3D());
        long count = shape3DService.countAllShape3Ds();
        Assert.assertTrue("Counter for 'Shape3D' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void Shape3DIntegrationTest.testFindShape3D() {
        Shape3D obj = dod.getRandomShape3D();
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to provide an identifier", id);
        obj = shape3DService.findShape3D(id);
        Assert.assertNotNull("Find method for 'Shape3D' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Shape3D' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void Shape3DIntegrationTest.testFindAllShape3Ds() {
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", dod.getRandomShape3D());
        long count = shape3DService.countAllShape3Ds();
        Assert.assertTrue("Too expensive to perform a find all test for 'Shape3D', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Shape3D> result = shape3DService.findAllShape3Ds();
        Assert.assertNotNull("Find all method for 'Shape3D' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Shape3D' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void Shape3DIntegrationTest.testFindShape3DEntries() {
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", dod.getRandomShape3D());
        long count = shape3DService.countAllShape3Ds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Shape3D> result = shape3DService.findShape3DEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Shape3D' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Shape3D' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void Shape3DIntegrationTest.testSaveShape3D() {
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", dod.getRandomShape3D());
        Shape3D obj = dod.getNewTransientShape3D(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Shape3D' identifier to be null", obj.getId());
        shape3DService.saveShape3D(obj);
        Assert.assertNotNull("Expected 'Shape3D' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void Shape3DIntegrationTest.testDeleteShape3D() {
        Shape3D obj = dod.getRandomShape3D();
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Shape3D' failed to provide an identifier", id);
        obj = shape3DService.findShape3D(id);
        shape3DService.deleteShape3D(obj);
        Assert.assertNull("Failed to remove 'Shape3D' with identifier '" + id + "'", shape3DService.findShape3D(id));
    }
    
}
