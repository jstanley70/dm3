// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.crystallography;

import com.lacunalabs.dm.data.physics.crystallography.LatticeVector3DDataOnDemand;
import com.lacunalabs.dm.data.physics.crystallography.LatticeVector3DIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.crystallography.LatticeVector3DService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect LatticeVector3DIntegrationTest_Roo_IntegrationTest {
    
    declare @type: LatticeVector3DIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: LatticeVector3DIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private LatticeVector3DDataOnDemand LatticeVector3DIntegrationTest.dod;
    
    @Autowired
    LatticeVector3DService LatticeVector3DIntegrationTest.latticeVector3DService;
    
    @Test
    public void LatticeVector3DIntegrationTest.testCountAllLatticeVector3Ds() {
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", dod.getRandomLatticeVector3D());
        long count = latticeVector3DService.countAllLatticeVector3Ds();
        Assert.assertTrue("Counter for 'LatticeVector3D' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void LatticeVector3DIntegrationTest.testFindLatticeVector3D() {
        LatticeVector3D obj = dod.getRandomLatticeVector3D();
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to provide an identifier", id);
        obj = latticeVector3DService.findLatticeVector3D(id);
        Assert.assertNotNull("Find method for 'LatticeVector3D' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'LatticeVector3D' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void LatticeVector3DIntegrationTest.testFindAllLatticeVector3Ds() {
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", dod.getRandomLatticeVector3D());
        long count = latticeVector3DService.countAllLatticeVector3Ds();
        Assert.assertTrue("Too expensive to perform a find all test for 'LatticeVector3D', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<LatticeVector3D> result = latticeVector3DService.findAllLatticeVector3Ds();
        Assert.assertNotNull("Find all method for 'LatticeVector3D' illegally returned null", result);
        Assert.assertTrue("Find all method for 'LatticeVector3D' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void LatticeVector3DIntegrationTest.testFindLatticeVector3DEntries() {
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", dod.getRandomLatticeVector3D());
        long count = latticeVector3DService.countAllLatticeVector3Ds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<LatticeVector3D> result = latticeVector3DService.findLatticeVector3DEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'LatticeVector3D' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'LatticeVector3D' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void LatticeVector3DIntegrationTest.testSaveLatticeVector3D() {
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", dod.getRandomLatticeVector3D());
        LatticeVector3D obj = dod.getNewTransientLatticeVector3D(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'LatticeVector3D' identifier to be null", obj.getId());
        latticeVector3DService.saveLatticeVector3D(obj);
        Assert.assertNotNull("Expected 'LatticeVector3D' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void LatticeVector3DIntegrationTest.testDeleteLatticeVector3D() {
        LatticeVector3D obj = dod.getRandomLatticeVector3D();
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'LatticeVector3D' failed to provide an identifier", id);
        obj = latticeVector3DService.findLatticeVector3D(id);
        latticeVector3DService.deleteLatticeVector3D(obj);
        Assert.assertNull("Failed to remove 'LatticeVector3D' with identifier '" + id + "'", latticeVector3DService.findLatticeVector3D(id));
    }
    
}
