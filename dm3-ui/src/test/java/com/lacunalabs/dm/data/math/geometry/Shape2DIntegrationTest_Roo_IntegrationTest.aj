// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.math.geometry;

import com.lacunalabs.dm.data.math.geometry.Shape2DDataOnDemand;
import com.lacunalabs.dm.data.math.geometry.Shape2DIntegrationTest;
import com.lacunalabs.dm.data.repository.math.geometry.Shape2DService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect Shape2DIntegrationTest_Roo_IntegrationTest {
    
    declare @type: Shape2DIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: Shape2DIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private Shape2DDataOnDemand Shape2DIntegrationTest.dod;
    
    @Autowired
    Shape2DService Shape2DIntegrationTest.shape2DService;
    
    @Test
    public void Shape2DIntegrationTest.testCountAllShape2Ds() {
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", dod.getRandomShape2D());
        long count = shape2DService.countAllShape2Ds();
        Assert.assertTrue("Counter for 'Shape2D' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void Shape2DIntegrationTest.testFindShape2D() {
        Shape2D obj = dod.getRandomShape2D();
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to provide an identifier", id);
        obj = shape2DService.findShape2D(id);
        Assert.assertNotNull("Find method for 'Shape2D' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Shape2D' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void Shape2DIntegrationTest.testFindAllShape2Ds() {
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", dod.getRandomShape2D());
        long count = shape2DService.countAllShape2Ds();
        Assert.assertTrue("Too expensive to perform a find all test for 'Shape2D', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Shape2D> result = shape2DService.findAllShape2Ds();
        Assert.assertNotNull("Find all method for 'Shape2D' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Shape2D' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void Shape2DIntegrationTest.testFindShape2DEntries() {
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", dod.getRandomShape2D());
        long count = shape2DService.countAllShape2Ds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Shape2D> result = shape2DService.findShape2DEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Shape2D' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Shape2D' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void Shape2DIntegrationTest.testSaveShape2D() {
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", dod.getRandomShape2D());
        Shape2D obj = dod.getNewTransientShape2D(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Shape2D' identifier to be null", obj.getId());
        shape2DService.saveShape2D(obj);
        Assert.assertNotNull("Expected 'Shape2D' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void Shape2DIntegrationTest.testDeleteShape2D() {
        Shape2D obj = dod.getRandomShape2D();
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Shape2D' failed to provide an identifier", id);
        obj = shape2DService.findShape2D(id);
        shape2DService.deleteShape2D(obj);
        Assert.assertNull("Failed to remove 'Shape2D' with identifier '" + id + "'", shape2DService.findShape2D(id));
    }
    
}
