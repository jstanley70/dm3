// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.fundamental;

import com.lacunalabs.dm.data.physics.fundamental.CompositeParticleDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.CompositeParticleIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.fundamental.CompositeParticleService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect CompositeParticleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CompositeParticleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CompositeParticleIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private CompositeParticleDataOnDemand CompositeParticleIntegrationTest.dod;
    
    @Autowired
    CompositeParticleService CompositeParticleIntegrationTest.compositeParticleService;
    
    @Test
    public void CompositeParticleIntegrationTest.testCountAllCompositeParticles() {
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", dod.getRandomCompositeParticle());
        long count = compositeParticleService.countAllCompositeParticles();
        Assert.assertTrue("Counter for 'CompositeParticle' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CompositeParticleIntegrationTest.testFindCompositeParticle() {
        CompositeParticle obj = dod.getRandomCompositeParticle();
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to provide an identifier", id);
        obj = compositeParticleService.findCompositeParticle(id);
        Assert.assertNotNull("Find method for 'CompositeParticle' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CompositeParticle' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CompositeParticleIntegrationTest.testFindAllCompositeParticles() {
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", dod.getRandomCompositeParticle());
        long count = compositeParticleService.countAllCompositeParticles();
        Assert.assertTrue("Too expensive to perform a find all test for 'CompositeParticle', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CompositeParticle> result = compositeParticleService.findAllCompositeParticles();
        Assert.assertNotNull("Find all method for 'CompositeParticle' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CompositeParticle' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CompositeParticleIntegrationTest.testFindCompositeParticleEntries() {
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", dod.getRandomCompositeParticle());
        long count = compositeParticleService.countAllCompositeParticles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CompositeParticle> result = compositeParticleService.findCompositeParticleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CompositeParticle' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CompositeParticle' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CompositeParticleIntegrationTest.testSaveCompositeParticle() {
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", dod.getRandomCompositeParticle());
        CompositeParticle obj = dod.getNewTransientCompositeParticle(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CompositeParticle' identifier to be null", obj.getId());
        compositeParticleService.saveCompositeParticle(obj);
        Assert.assertNotNull("Expected 'CompositeParticle' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CompositeParticleIntegrationTest.testDeleteCompositeParticle() {
        CompositeParticle obj = dod.getRandomCompositeParticle();
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CompositeParticle' failed to provide an identifier", id);
        obj = compositeParticleService.findCompositeParticle(id);
        compositeParticleService.deleteCompositeParticle(obj);
        Assert.assertNull("Failed to remove 'CompositeParticle' with identifier '" + id + "'", compositeParticleService.findCompositeParticle(id));
    }
    
}
