// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.instruments;

import com.lacunalabs.dm.data.physics.instruments.Aperature;
import com.lacunalabs.dm.data.physics.instruments.AperatureDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.instruments.AperatureService;
import com.lacunalabs.dm.shared.physics.fundamental.MetaDataType;
import java.math.BigDecimal;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect AperatureDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AperatureDataOnDemand: @Component;
    
    private Random AperatureDataOnDemand.rnd = new SecureRandom();
    
    private List<Aperature> AperatureDataOnDemand.data;
    
    @Autowired
    AperatureService AperatureDataOnDemand.aperatureService;
    
    public Aperature AperatureDataOnDemand.getNewTransientAperature(int index) {
        Aperature obj = new Aperature();
        setAperatureRadius(obj, index);
        setMetadata(obj, index);
        setMetadataType(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void AperatureDataOnDemand.setAperatureRadius(Aperature obj, int index) {
        BigDecimal aperatureRadius = BigDecimal.valueOf(index);
        obj.setAperatureRadius(aperatureRadius);
    }
    
    public void AperatureDataOnDemand.setMetadata(Aperature obj, int index) {
        String metadata = "metadata_" + index;
        obj.setMetadata(metadata);
    }
    
    public void AperatureDataOnDemand.setMetadataType(Aperature obj, int index) {
        MetaDataType metadataType = MetaDataType.class.getEnumConstants()[0];
        obj.setMetadataType(metadataType);
    }
    
    public void AperatureDataOnDemand.setName(Aperature obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public Aperature AperatureDataOnDemand.getSpecificAperature(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Aperature obj = data.get(index);
        BigInteger id = obj.getId();
        return aperatureService.findAperature(id);
    }
    
    public Aperature AperatureDataOnDemand.getRandomAperature() {
        init();
        Aperature obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return aperatureService.findAperature(id);
    }
    
    public boolean AperatureDataOnDemand.modifyAperature(Aperature obj) {
        return false;
    }
    
    public void AperatureDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = aperatureService.findAperatureEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Aperature' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Aperature>();
        for (int i = 0; i < 10; i++) {
            Aperature obj = getNewTransientAperature(i);
            try {
                aperatureService.saveAperature(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
