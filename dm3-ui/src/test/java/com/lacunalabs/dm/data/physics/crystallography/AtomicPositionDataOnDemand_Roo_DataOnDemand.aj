// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.crystallography;

import com.lacunalabs.dm.data.physics.crystallography.AtomicPosition;
import com.lacunalabs.dm.data.physics.crystallography.AtomicPositionDataOnDemand;
import com.lacunalabs.dm.data.physics.fundamental.CompositeParticleDataOnDemand;
import com.lacunalabs.dm.data.repository.physics.crystallography.AtomicPositionService;
import java.math.BigDecimal;
import java.math.BigInteger;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect AtomicPositionDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AtomicPositionDataOnDemand: @Component;
    
    private Random AtomicPositionDataOnDemand.rnd = new SecureRandom();
    
    private List<AtomicPosition> AtomicPositionDataOnDemand.data;
    
    @Autowired
    private CompositeParticleDataOnDemand AtomicPositionDataOnDemand.compositeParticleDataOnDemand;
    
    @Autowired
    AtomicPositionService AtomicPositionDataOnDemand.atomicPositionService;
    
    public AtomicPosition AtomicPositionDataOnDemand.getNewTransientAtomicPosition(int index) {
        AtomicPosition obj = new AtomicPosition();
        setOccupancy(obj, index);
        setPositionType(obj, index);
        setX(obj, index);
        setY(obj, index);
        setZ(obj, index);
        return obj;
    }
    
    public void AtomicPositionDataOnDemand.setOccupancy(AtomicPosition obj, int index) {
        BigDecimal occupancy = BigDecimal.valueOf(index);
        obj.setOccupancy(occupancy);
    }
    
    public void AtomicPositionDataOnDemand.setPositionType(AtomicPosition obj, int index) {
        String positionType = "positionType_" + index;
        obj.setPositionType(positionType);
    }
    
    public void AtomicPositionDataOnDemand.setX(AtomicPosition obj, int index) {
        BigDecimal x = BigDecimal.valueOf(index);
        obj.setX(x);
    }
    
    public void AtomicPositionDataOnDemand.setY(AtomicPosition obj, int index) {
        BigDecimal y = BigDecimal.valueOf(index);
        obj.setY(y);
    }
    
    public void AtomicPositionDataOnDemand.setZ(AtomicPosition obj, int index) {
        BigDecimal z = BigDecimal.valueOf(index);
        obj.setZ(z);
    }
    
    public AtomicPosition AtomicPositionDataOnDemand.getSpecificAtomicPosition(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AtomicPosition obj = data.get(index);
        BigInteger id = obj.getId();
        return atomicPositionService.findAtomicPosition(id);
    }
    
    public AtomicPosition AtomicPositionDataOnDemand.getRandomAtomicPosition() {
        init();
        AtomicPosition obj = data.get(rnd.nextInt(data.size()));
        BigInteger id = obj.getId();
        return atomicPositionService.findAtomicPosition(id);
    }
    
    public boolean AtomicPositionDataOnDemand.modifyAtomicPosition(AtomicPosition obj) {
        return false;
    }
    
    public void AtomicPositionDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = atomicPositionService.findAtomicPositionEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AtomicPosition' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AtomicPosition>();
        for (int i = 0; i < 10; i++) {
            AtomicPosition obj = getNewTransientAtomicPosition(i);
            try {
                atomicPositionService.saveAtomicPosition(obj);
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            data.add(obj);
        }
    }
    
}
