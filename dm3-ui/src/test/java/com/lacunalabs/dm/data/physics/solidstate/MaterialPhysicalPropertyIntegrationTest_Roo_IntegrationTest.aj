// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.lacunalabs.dm.data.physics.solidstate;

import com.lacunalabs.dm.data.physics.solidstate.MaterialPhysicalPropertyDataOnDemand;
import com.lacunalabs.dm.data.physics.solidstate.MaterialPhysicalPropertyIntegrationTest;
import com.lacunalabs.dm.data.repository.physics.solidstate.MaterialPhysicalPropertyService;
import java.math.BigInteger;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

privileged aspect MaterialPhysicalPropertyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MaterialPhysicalPropertyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MaterialPhysicalPropertyIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    @Autowired
    private MaterialPhysicalPropertyDataOnDemand MaterialPhysicalPropertyIntegrationTest.dod;
    
    @Autowired
    MaterialPhysicalPropertyService MaterialPhysicalPropertyIntegrationTest.materialPhysicalPropertyService;
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testCountAllMaterialPhysicalPropertys() {
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", dod.getRandomMaterialPhysicalProperty());
        long count = materialPhysicalPropertyService.countAllMaterialPhysicalPropertys();
        Assert.assertTrue("Counter for 'MaterialPhysicalProperty' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testFindMaterialPhysicalProperty() {
        MaterialPhysicalProperty obj = dod.getRandomMaterialPhysicalProperty();
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to provide an identifier", id);
        obj = materialPhysicalPropertyService.findMaterialPhysicalProperty(id);
        Assert.assertNotNull("Find method for 'MaterialPhysicalProperty' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MaterialPhysicalProperty' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testFindAllMaterialPhysicalPropertys() {
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", dod.getRandomMaterialPhysicalProperty());
        long count = materialPhysicalPropertyService.countAllMaterialPhysicalPropertys();
        Assert.assertTrue("Too expensive to perform a find all test for 'MaterialPhysicalProperty', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MaterialPhysicalProperty> result = materialPhysicalPropertyService.findAllMaterialPhysicalPropertys();
        Assert.assertNotNull("Find all method for 'MaterialPhysicalProperty' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MaterialPhysicalProperty' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testFindMaterialPhysicalPropertyEntries() {
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", dod.getRandomMaterialPhysicalProperty());
        long count = materialPhysicalPropertyService.countAllMaterialPhysicalPropertys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MaterialPhysicalProperty> result = materialPhysicalPropertyService.findMaterialPhysicalPropertyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MaterialPhysicalProperty' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MaterialPhysicalProperty' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testSaveMaterialPhysicalProperty() {
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", dod.getRandomMaterialPhysicalProperty());
        MaterialPhysicalProperty obj = dod.getNewTransientMaterialPhysicalProperty(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'MaterialPhysicalProperty' identifier to be null", obj.getId());
        materialPhysicalPropertyService.saveMaterialPhysicalProperty(obj);
        Assert.assertNotNull("Expected 'MaterialPhysicalProperty' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MaterialPhysicalPropertyIntegrationTest.testDeleteMaterialPhysicalProperty() {
        MaterialPhysicalProperty obj = dod.getRandomMaterialPhysicalProperty();
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to initialize correctly", obj);
        BigInteger id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MaterialPhysicalProperty' failed to provide an identifier", id);
        obj = materialPhysicalPropertyService.findMaterialPhysicalProperty(id);
        materialPhysicalPropertyService.deleteMaterialPhysicalProperty(obj);
        Assert.assertNull("Failed to remove 'MaterialPhysicalProperty' with identifier '" + id + "'", materialPhysicalPropertyService.findMaterialPhysicalProperty(id));
    }
    
}
